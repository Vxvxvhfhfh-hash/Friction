{
  "name": "whatsapp-rp-bot",
  "version": "1.0.0",
  "description": "Application web RP avec IA Gemini déployable sur Vercel",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "vercel dev",
    "build": "echo 'Build completed'",
    "deploy": "vercel --prod",
    "setup": "node scripts/setup.js",
    "test": "echo \"Tests not implemented yet\" && exit 0",
    "lint": "echo \"Linting not configured yet\" && exit 0"
  },
  "keywords": ["whatsapp", "bot", "roleplay", "gemini", "ai"],
  "author": "RP Bot Creator",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.2.1",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "vercel": "^32.0.0"
  }
}